Meteor.methods({
  createNewApplication: function(name, type) {
  // Select Device
  // Create monitor
  // Create pool
  // Create profiles
  // Create Virtual
  // Create GTM virtual
  // Create GTM Pool
  // Create GTM WIP
  }
});
